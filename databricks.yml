# This is a Databricks asset bundle definition for lineage_ops.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: lineage_ops
  uuid: 85eb3975-8d37-4da5-8bd9-57a071186f83

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-984752964297111.11.azuredatabricks.net
    permissions:
      - user_name: cinyoung.hur@databricks.com
        level: CAN_MANAGE
      - user_name: allison.chau@databricks.com
        level: CAN_MANAGE

  prod:
    mode: production
    workspace:
      host: https://adb-984752964297111.11.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/cinyoung.hur@databricks.com to make sure we only have a single copy.
      root_path: /Workspace/Users/cinyoung.hur@databricks.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: cinyoung.hur@databricks.com
        level: CAN_MANAGE
